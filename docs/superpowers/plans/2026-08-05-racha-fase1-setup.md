# Racha — Fase 1: Setup del proyecto — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. NOTE: this plan is executed **inline, conversationally, one step at a time, waiting for explicit user confirmation before each step**, per the "Nota importante para Claude Code" in the project README — the user has never programmed before and several steps require manual action on their phone or in a web browser that only they can perform.

**Goal:** Get a working Expo app, connected to Supabase, with email login and 5 empty tabs styled with the project's visual identity, running on the user's iPhone via Expo Go, with the code pushed to GitHub.

**Architecture:** Expo-managed React Native app (JavaScript, no TypeScript, to minimize concepts for a beginner). React Navigation bottom-tabs for the 5 sections. A single Supabase client module used across the app. A minimal auth gate in `App.js` that shows either the login screen or the tab navigator based on session state.

**Tech Stack:** Expo (SDK latest via `create-expo-app`), React Navigation (`@react-navigation/native`, `@react-navigation/bottom-tabs`), `@supabase/supabase-js`, `@react-native-async-storage/async-storage`, `react-native-url-polyfill`.

## Global Constraints

- Explain every step in plain language before running it (what it does and why) — from README.
- One step at a time; wait for user confirmation before moving to the next — from README.
- Call out manual actions explicitly (install an app, tap a button, scan a QR, create an account) and say exactly what to do — from README.
- If something fails, explain the failure in simple terms before attempting a fix — from README.
- Don't assume familiarity with the terminal, Xcode, Android Studio, etc. — explain as needed — from README.
- Language for the app's user-facing text and for talking to the user: Spanish (matches README).
- Navigation library must be React Navigation, not `expo-router` — from README's tech stack section.
- No TypeScript — chosen in brainstorming to reduce concepts for a first-time programmer.
- Phase 1 auth is email/password only via Supabase's built-in `auth.users`; no custom `profiles` table yet (deferred to Module 5) — scope decision from design doc.

---

## File Structure

- `App.js` — root component: checks Supabase session, renders `AuthScreen` or `TabNavigator`.
- `src/lib/supabase.js` — Supabase client instance, reads URL/key from env vars.
- `src/navigation/TabNavigator.js` — bottom-tab navigator wiring the 5 placeholder screens.
- `src/screens/RachaScreen.js`, `EjerciciosScreen.js`, `FisicoScreen.js`, `ObjetivoScreen.js`, `PerfilScreen.js` — placeholder screens, one per tab.
- `src/screens/AuthScreen.js` — single screen with email/password fields and a toggle between "Iniciar sesión" and "Crear cuenta".
- `.env` — `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY` (not committed).
- `.gitignore` — extended to make sure `.env` is excluded.

---

## Task 1: Crear el proyecto Expo y verlo correr en el iPhone

**Files:**
- Create: entire project scaffold (generated by `create-expo-app`) at `C:\GYMAPP`

**Interfaces:**
- Produces: a runnable Expo project with `npx expo start` available as the dev command for every later task.

- [ ] **Step 1: Explicar y crear el proyecto Expo**

Explicar: vamos a generar la base de la app con una herramienta llamada Expo. Esto crea una carpeta con todos los archivos iniciales.

Run (dentro de `C:\GYMAPP`, que ya está vacía salvo el README y `docs/`):
```bash
npx create-expo-app@latest . --template blank
```
Nota: se usa `.` porque `C:\GYMAPP` ya es la carpeta del proyecto (no hay que crear una subcarpeta nueva).

- [ ] **Step 2: Confirmar que se creó bien**

Run: `dir` (o revisar que exista `package.json`, `App.js`, `app.json`)
Expected: los archivos están presentes, sin errores en la consola del paso anterior.

- [ ] **Step 3: Acción manual — instalar Expo Go en el iPhone**

Avisar al usuario: "Andá a la App Store en tu iPhone, buscá 'Expo Go' e instalala (es gratis). Avisame cuando la tengas instalada."
Esperar confirmación del usuario antes de seguir.

- [ ] **Step 4: Levantar el servidor de desarrollo**

Explicar: esto prende un servidor en la compu que le manda la app al celular en tiempo real.

Run: `npx expo start`
Expected: aparece un código QR en la terminal.

- [ ] **Step 5: Acción manual — abrir la app en el celular**

Avisar al usuario: "Abrí la cámara de tu iPhone y apuntá al código QR que apareció en la terminal. Te va a aparecer una notificación arriba, tocala y se va a abrir Expo Go con la app."
Esperar confirmación de que ve la pantalla default de Expo en su celular.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: scaffold Expo project"
```

---

## Task 2: Navegación de tabs (Racha / Ejercicios / Físico / Objetivo / Perfil)

**Files:**
- Create: `src/screens/RachaScreen.js`
- Create: `src/screens/EjerciciosScreen.js`
- Create: `src/screens/FisicoScreen.js`
- Create: `src/screens/ObjetivoScreen.js`
- Create: `src/screens/PerfilScreen.js`
- Create: `src/navigation/TabNavigator.js`
- Modify: `App.js`

**Interfaces:**
- Consumes: nothing from Task 1 besides the running project.
- Produces: `TabNavigator` component (default export from `src/navigation/TabNavigator.js`), rendered directly by `App.js`. Later tasks (auth) will wrap this instead of rendering it unconditionally.

- [ ] **Step 1: Instalar las librerías de navegación**

Explicar: React Navigation es la librería que arma la barra de pestañas de abajo.

Run:
```bash
npx expo install @react-navigation/native @react-navigation/bottom-tabs react-native-screens react-native-safe-area-context
```

- [ ] **Step 2: Crear las 5 pantallas placeholder**

Create `src/screens/RachaScreen.js`:
```javascript
import { View, Text, StyleSheet } from 'react-native';

export default function RachaScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Racha</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  text: { fontSize: 20 },
});
```

Create `src/screens/EjerciciosScreen.js` (mismo patrón, cambiar el texto a "Ejercicios"):
```javascript
import { View, Text, StyleSheet } from 'react-native';

export default function EjerciciosScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Ejercicios</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  text: { fontSize: 20 },
});
```

Create `src/screens/FisicoScreen.js`:
```javascript
import { View, Text, StyleSheet } from 'react-native';

export default function FisicoScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Físico</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  text: { fontSize: 20 },
});
```

Create `src/screens/ObjetivoScreen.js`:
```javascript
import { View, Text, StyleSheet } from 'react-native';

export default function ObjetivoScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Objetivo</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  text: { fontSize: 20 },
});
```

Create `src/screens/PerfilScreen.js`:
```javascript
import { View, Text, StyleSheet } from 'react-native';

export default function PerfilScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Perfil</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  text: { fontSize: 20 },
});
```

- [ ] **Step 3: Crear el TabNavigator**

Create `src/navigation/TabNavigator.js`:
```javascript
import { NavigationContainer } from '@react-navigation/native';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import RachaScreen from '../screens/RachaScreen';
import EjerciciosScreen from '../screens/EjerciciosScreen';
import FisicoScreen from '../screens/FisicoScreen';
import ObjetivoScreen from '../screens/ObjetivoScreen';
import PerfilScreen from '../screens/PerfilScreen';

const Tab = createBottomTabNavigator();

export default function TabNavigator() {
  return (
    <NavigationContainer>
      <Tab.Navigator>
        <Tab.Screen name="Racha" component={RachaScreen} />
        <Tab.Screen name="Ejercicios" component={EjerciciosScreen} />
        <Tab.Screen name="Físico" component={FisicoScreen} />
        <Tab.Screen name="Objetivo" component={ObjetivoScreen} />
        <Tab.Screen name="Perfil" component={PerfilScreen} />
      </Tab.Navigator>
    </NavigationContainer>
  );
}
```

- [ ] **Step 4: Conectar en App.js**

Modify `App.js` to be exactly:
```javascript
import TabNavigator from './src/navigation/TabNavigator';

export default function App() {
  return <TabNavigator />;
}
```

- [ ] **Step 5: Verificación manual**

Explicar: como el servidor de `npx expo start` ya está corriendo, la app en el celular se actualiza sola al guardar los archivos.
Expected: el usuario ve 5 pestañas abajo (Racha, Ejercicios, Físico, Objetivo, Perfil) y al tocar cada una aparece el texto correspondiente en el centro.
Esperar confirmación del usuario.

- [ ] **Step 6: Commit**

```bash
git add src App.js
git commit -m "feat: add bottom tab navigation with 5 placeholder screens"
```

---

## Task 3: Crear el proyecto en Supabase

**Files:** ninguno (trabajo en el sitio web de Supabase) + creación de `.env`

**Interfaces:**
- Produces: una URL de proyecto Supabase y una "anon key" (clave pública), que Task 4 va a consumir como `EXPO_PUBLIC_SUPABASE_URL` y `EXPO_PUBLIC_SUPABASE_ANON_KEY`.

- [ ] **Step 1: Acción manual — crear el proyecto en supabase.com**

Avisar al usuario paso a paso:
1. Entrá a https://supabase.com/dashboard y logueate con tu cuenta.
2. Tocá "New Project".
3. Ponele de nombre "racha" (o el que quieras), elegí una contraseña para la base de datos (guardala en un lugar seguro, no hace falta recordarla de memoria) y una región cercana (por ejemplo, la más cercana a tu país).
4. Tocá "Create new project" y esperá 1-2 minutos a que termine de crearse.

Esperar confirmación del usuario de que el proyecto está creado.

- [ ] **Step 2: Acción manual — obtener las credenciales**

Avisar al usuario:
1. Dentro del proyecto, andá a "Project Settings" (ícono de engranaje) → "API".
2. Copiá el valor de "Project URL".
3. Copiá el valor de "anon public" (dentro de "Project API Keys").

Pedirle al usuario que pegue esos dos valores en el chat (o confirme que los tiene a mano).

- [ ] **Step 3: Crear el archivo `.env`**

Create `.env` (usando los valores que pasó el usuario):
```
EXPO_PUBLIC_SUPABASE_URL=<valor pegado por el usuario>
EXPO_PUBLIC_SUPABASE_ANON_KEY=<valor pegado por el usuario>
```

- [ ] **Step 4: Asegurarse de que `.env` no se suba a GitHub**

Modify `.gitignore` — agregar al final (si no está ya):
```
.env
```

- [ ] **Step 5: Reiniciar el servidor de Expo**

Explicar: Expo solo lee el `.env` cuando arranca, así que hay que reiniciarlo.

Run: cortar `npx expo start` (Ctrl+C) y volver a correr `npx expo start`.

- [ ] **Step 6: Commit**

```bash
git add .gitignore
git commit -m "chore: ignore .env"
```
(El `.env` en sí NUNCA se commitea — por eso no aparece en el `git add`.)

---

## Task 4: Conectar la app a Supabase

**Files:**
- Create: `src/lib/supabase.js`

**Interfaces:**
- Consumes: `EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY` (de Task 3).
- Produces: `supabase` (named export) — cliente de Supabase, usado por Task 5 para login/registro.

- [ ] **Step 1: Instalar dependencias**

Run:
```bash
npx expo install @supabase/supabase-js @react-native-async-storage/async-storage react-native-url-polyfill
```

- [ ] **Step 2: Crear el cliente de Supabase**

Create `src/lib/supabase.js`:
```javascript
import 'react-native-url-polyfill/auto';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.EXPO_PUBLIC_SUPABASE_URL;
const supabaseAnonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseAnonKey, {
  auth: {
    storage: AsyncStorage,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
});
```

- [ ] **Step 3: Verificar que no hay errores al arrancar**

Run: (si `npx expo start` no está corriendo) `npx expo start`
Expected: la app sigue abriendo en el celular sin pantalla roja de error.

- [ ] **Step 4: Commit**

```bash
git add src/lib/supabase.js
git commit -m "feat: add Supabase client"
```

---

## Task 5: Login y registro con email

**Files:**
- Create: `src/screens/AuthScreen.js`
- Modify: `App.js`

**Interfaces:**
- Consumes: `supabase` de `src/lib/supabase.js` (Task 4), `TabNavigator` de `src/navigation/TabNavigator.js` (Task 2).
- Produces: comportamiento final de `App.js` — muestra `AuthScreen` si no hay sesión, `TabNavigator` si la hay.

- [ ] **Step 1: Crear la pantalla de autenticación**

Create `src/screens/AuthScreen.js`:
```javascript
import { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet, Alert } from 'react-native';
import { supabase } from '../lib/supabase';

export default function AuthScreen() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const [modoRegistro, setModoRegistro] = useState(false);

  async function handleSubmit() {
    setLoading(true);
    const { error } = modoRegistro
      ? await supabase.auth.signUp({ email, password })
      : await supabase.auth.signInWithPassword({ email, password });
    setLoading(false);
    if (error) {
      Alert.alert('Error', error.message);
    }
  }

  return (
    <View style={styles.container}>
      <Text style={styles.title}>{modoRegistro ? 'Crear cuenta' : 'Iniciar sesión'}</Text>
      <TextInput
        style={styles.input}
        placeholder="Email"
        autoCapitalize="none"
        keyboardType="email-address"
        value={email}
        onChangeText={setEmail}
      />
      <TextInput
        style={styles.input}
        placeholder="Contraseña"
        secureTextEntry
        value={password}
        onChangeText={setPassword}
      />
      <Button
        title={loading ? 'Cargando...' : modoRegistro ? 'Crear cuenta' : 'Iniciar sesión'}
        onPress={handleSubmit}
        disabled={loading}
      />
      <Text style={styles.toggle} onPress={() => setModoRegistro(!modoRegistro)}>
        {modoRegistro ? '¿Ya tenés cuenta? Iniciar sesión' : '¿No tenés cuenta? Crear una'}
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 24 },
  title: { fontSize: 24, marginBottom: 24, textAlign: 'center' },
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    padding: 12,
    marginBottom: 12,
  },
  toggle: { marginTop: 16, textAlign: 'center', color: '#2563eb' },
});
```

- [ ] **Step 2: Conectar el estado de sesión en App.js**

Modify `App.js` to be exactly:
```javascript
import { useEffect, useState } from 'react';
import { View, ActivityIndicator } from 'react-native';
import { supabase } from './src/lib/supabase';
import TabNavigator from './src/navigation/TabNavigator';
import AuthScreen from './src/screens/AuthScreen';

export default function App() {
  const [session, setSession] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      setSession(session);
      setLoading(false);
    });

    const { data: listener } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session);
    });

    return () => listener.subscription.unsubscribe();
  }, []);

  if (loading) {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <ActivityIndicator size="large" />
      </View>
    );
  }

  return session ? <TabNavigator /> : <AuthScreen />;
}
```

- [ ] **Step 3: Verificación manual — registro**

Explicar al usuario: "Ahora en el celular deberías ver la pantalla de login. Tocá '¿No tenés cuenta? Crear una', poné un email real tuyo y una contraseña, y tocá 'Crear cuenta'."
Expected: no aparece ningún error, y (según la configuración de Supabase) puede pedir confirmar el email o entrar directo a los tabs.

- [ ] **Step 4: Verificación manual — confirmar en el dashboard de Supabase**

Avisar al usuario: "Andá a tu proyecto en supabase.com → Authentication → Users. Deberías ver el email que acabás de registrar en la lista."
Esperar confirmación.

- [ ] **Step 5: Commit**

```bash
git add src/screens/AuthScreen.js App.js
git commit -m "feat: add email login/signup screen and session gate"
```

---

## Task 6: Importar la identidad visual (Claude Design)

**Files:** los que genere la importación (componentes/estilos de la identidad visual); se documentan cuando se ejecute este paso, ya que dependen del contenido real del proyecto de diseño.

**Interfaces:**
- Consumes: pantallas y navegación ya existentes (Tasks 2 y 5).
- Produces: estilos/colores/tipografía aplicados a las pantallas existentes.

- [ ] **Step 1: Explicar qué se va a hacer**

Explicar al usuario: "Vos ya tenés un diseño visual armado para esta app en Claude Design (colores, tipografía). Ahora le voy a pedir a esa herramienta que traiga ese diseño para aplicarlo a las pantallas que ya armamos."

- [ ] **Step 2: Ejecutar la importación**

Usar el MCP de diseño con el prompt indicado en el README (sección "Identidad visual"):
```
Use the claude_design MCP (https://api.anthropic.com/v1/design/mcp, auth via /design-login) to import this project:
https://claude.ai/design/p/8e8abfbf-685f-4c2b-8e6c-7ef7f8f5ae4b?file=Racha+-+Identidad+Visual.dc.html
Focus on these files (the whole project is readable):
- `Racha - Identidad Visual.dc.html`
Also read these files the selection imports:
- `ios-frame.jsx`
- `support.js`
Implement: `Racha - Identidad Visual.dc.html`
```
Si hace falta login (`/design-login`), avisar al usuario y esperar a que confirme que inició sesión en el navegador que se abra.

- [ ] **Step 3: Aplicar los estilos a las pantallas existentes**

Ajustar `src/screens/*.js` y `src/navigation/TabNavigator.js` para usar los colores/tipografía importados (se define el detalle exacto una vez que se vea el contenido real importado, ya que depende de lo que traiga el proyecto de diseño).

- [ ] **Step 4: Verificación manual**

Expected: el usuario ve las 5 pantallas y la barra de tabs con los colores/tipografía del diseño, no con el estilo por defecto de React Native.
Esperar confirmación.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: apply visual identity from Claude Design"
```

---

## Task 7: Subir el proyecto a GitHub

**Files:** ninguno (configuración de repo remoto)

**Interfaces:** ninguna (último paso de la fase).

- [ ] **Step 1: Acción manual — crear el repositorio en GitHub**

Avisar al usuario:
1. Entrá a https://github.com/new
2. Nombre del repositorio: "racha" (o el que prefieras).
3. Dejalo "Private" si querés que no lo vea nadie más.
4. NO marques "Add a README" (ya tenemos uno) — dejá el repo vacío.
5. Tocá "Create repository" y copiá la URL que te muestra (termina en `.git`).

- [ ] **Step 2: Conectar el repo local con GitHub**

Run (reemplazando `<URL>` por la que copió el usuario):
```bash
git remote add origin <URL>
git branch -M main
git push -u origin main
```
Nota: la primera vez puede abrir el navegador para que el usuario inicie sesión en GitHub y autorice — avisar que es esperable y esperar confirmación de que terminó.

- [ ] **Step 3: Verificación manual**

Avisar al usuario: "Refrescá la página de tu repositorio en GitHub. Deberías ver todos los archivos del proyecto ahí."
Esperar confirmación.

---

## Fin de la Fase 1

Al completar las 7 tareas: la app corre en el iPhone del usuario vía Expo Go, tiene navegación de 5 tabs con la identidad visual aplicada, login/registro funcional contra Supabase, y el código está en GitHub. Las fases siguientes (Módulos 1-5) se planifican por separado cuando se llegue a ellas.
